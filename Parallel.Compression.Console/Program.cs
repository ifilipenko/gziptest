using Parallel.Compression.Cli;

namespace Parallel.Compression.Console
{
    internal static class Program
    {
        private static int Main(string[] args)
        {
            return EntryPoint.Run(args);
        }
    }
}